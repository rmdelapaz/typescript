# React TypeScript Course - Development Journal

## Project Location
\\wsl$\Ubuntu\home\practicalace\projects\typescript

## Transcript Archive
/mnt/transcripts/

---

## 2024-11-27 - PENDING BUG FIX

### Issue: Lesson 1.4 - Unescaped Generic Syntax
**File:** lesson_1_4_functions_in_typescript.html
**Location:** Section 8 (Hands-on Practice), Step 2: Create the Pipeline Class
**Specific area:** After the comment `// Reduce to single value` (user reported around line 2329)

**Problem:** Unescaped `<U>` generic type syntax is being interpreted as an HTML tag, causing underlining to turn on and never turn off for the rest of the page.

**Fix needed:** Find and escape all generic angle brackets in that code block:
- `<U>` → `&lt;U&gt;`
- `<T, U>` → `&lt;T, U&gt;`
- Similar patterns

**Status:** ✅ FIXED (2024-11-26)

**Fix Applied:** Escaped the following unescaped generic syntax in the DataPipeline class:
- `reduce<U>` → `reduce&lt;U&gt;`
- `Reducer<T, U>` → `Reducer&lt;T, U&gt;`
- `groupBy<K extends keyof T>` → `groupBy&lt;K extends keyof T&gt;`
- `Map<T[K], T[]>` → `Map&lt;T[K], T[]&gt;`

**Note:** The `map` method generics in Step 2 were already fixed earlier.

---

## Completed Work

### Visual Enhancements (SVG diagrams added)
- Lesson 1.1: 7 SVGs
- Lesson 1.2: 7 SVGs  
- Lesson 1.3: 5 SVGs + mermaid fix
- Lesson 1.4: 6 SVGs

### Previous Bug Fixes
- Lesson 1.4: Fixed `map<U>` method generics in DataPipeline class (escaped to `map&lt;U&gt;`)
